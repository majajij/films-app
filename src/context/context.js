import React from "react";
const updateFilmContext = React.createContext({
  //films: DATA,
  //favoritFilms: DATA.filter((e) => e.isFav),
  //filtredFilms: DATA
});

export default updateFilmContext;
